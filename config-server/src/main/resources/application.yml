server:
    port: 8888

spring:
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/marcos-araujo/microservice-repo
          search-paths: provider, store, shipping